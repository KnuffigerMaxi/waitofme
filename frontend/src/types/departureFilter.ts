// Importiere alle notwendigen Typen
import { Departure } from '../types/Departure';

// Definiere eine Funktion, die die Abfahrten filtert
export function departureFilter(departures: Departure[], filterParam: string): Departure[] {
  // Füge hier den Code zum Filtern der Abfahrten hinzu
  return filteredDepartures;
}